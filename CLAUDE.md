# Claude Code adapter

@AGENTS.md

This file is intentionally a thin redirect (single source of truth = root `AGENTS.md`, the VibeCode Constitution). Operational build/test/style instructions live in `app/AGENTS.md` (nearest-file precedence). Domain invariants live in `.agent/FIRST_PRINCIPLES.md`.
